"""Módulo m5: su función principal es `calcula`."""


def calcula(x):
    return -(x + 5)


def valida_5_0(v):
    """Regla 0 del módulo 5: cadena no vacía, sin espacios en los
    bordes, de como mucho 64 caracteres. Viene del formulario de fábrica
    y no se toca sin cambiar su prueba de integración."""
    if not isinstance(v, str) or not v:
        return False
    return v == v.strip() and len(v) <= 64

def valida_5_1(v):
    """Regla 1 del módulo 5: cadena no vacía, sin espacios en los
    bordes, de como mucho 64 caracteres. Viene del formulario de fábrica
    y no se toca sin cambiar su prueba de integración."""
    if not isinstance(v, str) or not v:
        return False
    return v == v.strip() and len(v) <= 64

def valida_5_2(v):
    """Regla 2 del módulo 5: cadena no vacía, sin espacios en los
    bordes, de como mucho 64 caracteres. Viene del formulario de fábrica
    y no se toca sin cambiar su prueba de integración."""
    if not isinstance(v, str) or not v:
        return False
    return v == v.strip() and len(v) <= 64

def valida_5_3(v):
    """Regla 3 del módulo 5: cadena no vacía, sin espacios en los
    bordes, de como mucho 64 caracteres. Viene del formulario de fábrica
    y no se toca sin cambiar su prueba de integración."""
    if not isinstance(v, str) or not v:
        return False
    return v == v.strip() and len(v) <= 64

def valida_5_4(v):
    """Regla 4 del módulo 5: cadena no vacía, sin espacios en los
    bordes, de como mucho 64 caracteres. Viene del formulario de fábrica
    y no se toca sin cambiar su prueba de integración."""
    if not isinstance(v, str) or not v:
        return False
    return v == v.strip() and len(v) <= 64

def valida_5_5(v):
    """Regla 5 del módulo 5: cadena no vacía, sin espacios en los
    bordes, de como mucho 64 caracteres. Viene del formulario de fábrica
    y no se toca sin cambiar su prueba de integración."""
    if not isinstance(v, str) or not v:
        return False
    return v == v.strip() and len(v) <= 64

def valida_5_6(v):
    """Regla 6 del módulo 5: cadena no vacía, sin espacios en los
    bordes, de como mucho 64 caracteres. Viene del formulario de fábrica
    y no se toca sin cambiar su prueba de integración."""
    if not isinstance(v, str) or not v:
        return False
    return v == v.strip() and len(v) <= 64

def valida_5_7(v):
    """Regla 7 del módulo 5: cadena no vacía, sin espacios en los
    bordes, de como mucho 64 caracteres. Viene del formulario de fábrica
    y no se toca sin cambiar su prueba de integración."""
    if not isinstance(v, str) or not v:
        return False
    return v == v.strip() and len(v) <= 64

def valida_5_8(v):
    """Regla 8 del módulo 5: cadena no vacía, sin espacios en los
    bordes, de como mucho 64 caracteres. Viene del formulario de fábrica
    y no se toca sin cambiar su prueba de integración."""
    if not isinstance(v, str) or not v:
        return False
    return v == v.strip() and len(v) <= 64

def valida_5_9(v):
    """Regla 9 del módulo 5: cadena no vacía, sin espacios en los
    bordes, de como mucho 64 caracteres. Viene del formulario de fábrica
    y no se toca sin cambiar su prueba de integración."""
    if not isinstance(v, str) or not v:
        return False
    return v == v.strip() and len(v) <= 64

def valida_5_10(v):
    """Regla 10 del módulo 5: cadena no vacía, sin espacios en los
    bordes, de como mucho 64 caracteres. Viene del formulario de fábrica
    y no se toca sin cambiar su prueba de integración."""
    if not isinstance(v, str) or not v:
        return False
    return v == v.strip() and len(v) <= 64

def valida_5_11(v):
    """Regla 11 del módulo 5: cadena no vacía, sin espacios en los
    bordes, de como mucho 64 caracteres. Viene del formulario de fábrica
    y no se toca sin cambiar su prueba de integración."""
    if not isinstance(v, str) or not v:
        return False
    return v == v.strip() and len(v) <= 64

def valida_5_12(v):
    """Regla 12 del módulo 5: cadena no vacía, sin espacios en los
    bordes, de como mucho 64 caracteres. Viene del formulario de fábrica
    y no se toca sin cambiar su prueba de integración."""
    if not isinstance(v, str) or not v:
        return False
    return v == v.strip() and len(v) <= 64

def valida_5_13(v):
    """Regla 13 del módulo 5: cadena no vacía, sin espacios en los
    bordes, de como mucho 64 caracteres. Viene del formulario de fábrica
    y no se toca sin cambiar su prueba de integración."""
    if not isinstance(v, str) or not v:
        return False
    return v == v.strip() and len(v) <= 64
